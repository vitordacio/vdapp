import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {},
  error: {
    fontSize: 14,
    textAlign: 'center',
    color: 'red',
    height: 19,
    marginTop: -19,
  },
});

export default styles;
