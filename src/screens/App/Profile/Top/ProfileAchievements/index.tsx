import { AppProps } from '@routes/App/app.routes';
import React from 'react';
import { Text, View } from 'react-native';

const ProfileAchievements: React.FC<AppProps> = () => {
  return (
    <View>
      <Text>Achievements</Text>
    </View>
  );
};

export default ProfileAchievements;
