import { AppProps } from '@routes/App/app.routes';
import React from 'react';
import { Text, View } from 'react-native';

const UserReactsSent: React.FC<AppProps> = () => {
  return (
    <View>
      <Text>ReactsSent</Text>
    </View>
  );
};

export default UserReactsSent;
